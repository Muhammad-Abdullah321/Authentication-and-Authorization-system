# Authentication-and-Authorization-system
Secure authentication and authorization API built with Node.js, Express.js, and MongoDB. Features JWT access/refresh tokens, bcrypt, HTTP-only cookies, sessions, OTP verification, Google OAuth, protected Todo APIs, user-specific authorization, and Stripe payment integration.
